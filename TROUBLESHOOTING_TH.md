# 🔧 วิธีแก้ปัญหา CORS และ API Key Authentication Error

## ปัญหาที่พบ

### ปัญหาที่ 1: CORS Error
```
Access to fetch has been blocked by CORS policy: 
No 'Access-Control-Allow-Origin' header is present
```

### ปัญหาที่ 2: API Key Error
```
"error": "Unauthorized: Invalid API Key"
```

## สาเหตุหลัก
1. Google Apps Script ยังไม่ได้ Deploy ถูกต้อง
2. Deploy แล้วแต่ไม่ได้เลือก "Anyone" access
3. API Key ไม่ตรงกัน

---

## 🚨 วิธีแก้ไข CORS Error (สำคัญที่สุด!)

### ขั้นที่ 1: อัพเดท Code.gs

1. เปิด Google Apps Script
2. เพิ่มฟังก์ชัน `doOptions` ไว้หลัง API_KEY:

```javascript
/**
 * Handle OPTIONS requests for CORS preflight
 */
function doOptions(e) {
  return createResponse({}, 200);
}
```

3. บันทึกไฟล์ (Ctrl+S)

### ขั้นที่ 2: Deploy ใหม่ (สำคัญมาก!)

1. คลิก **Deploy > Manage deployments**
2. คลิกไอคอน **Edit** (รูปดินสอ) ข้างเวอร์ชันปัจจุบัน
3. คลิก **Version > New version**
4. ใส่คำอธิบาย: "Added CORS support"
5. **สำคัญ!** ตรวจสอบว่า:
   - Execute as: **Me** (your email)
   - Who has access: **Anyone** ⬅️ **ต้องเป็น Anyone!**
6. คลิก **Deploy**
7. คัดลอก **Web app URL** ใหม่

### ขั้นที่ 3: ทดสอบว่า Deploy สำเร็จ

1. เปิด Web app URL ในเบราว์เซอร์
2. คุณจะเห็นข้อความ: `{"error":"Unauthorized: Invalid API Key","success":false}`
3. ถ้าเห็นข้อความนี้แสดงว่า Deploy สำเร็จแล้ว!

---

## 🔑 วิธีแก้ไข API Key Error

### ขั้นที่ 1: ตรวจสอบ API Key ใน Google Apps Script

1. เปิด Google Sheet ของคุณ
2. ไปที่ **Extensions > Apps Script**
3. คลิกที่ไอคอน **Project Settings** (เฟือง) ด้านซ้าย
4. เลื่อนลงไปที่ส่วน **Script Properties**
5. ดูค่า `API_KEY` ที่ตั้งไว้ (หรือสร้างใหม่ถ้ายังไม่มี)

### ขั้นที่ 2: สร้าง API Key ที่ปลอดภัย (ถ้ายังไม่มี)

เปิด PowerShell และรันคำสั่ง:
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

คัดลอกค่าที่ได้ เช่น: `xK8+3mP9qR4vN2jL5tW7aZ1cY6eB0fH8`

### ขั้นที่ 3: ตั้งค่าใน Google Apps Script

ใน Script Properties:
1. คลิก **Add script property**
2. Property: `API_KEY`
3. Value: วาง API Key ที่สร้างไว้
4. คลิก **Save script properties**

### ขั้นที่ 4: อัพเดตไฟล์ .env.local

เปิดไฟล์ `.env.local` และแก้ไขดังนี้:

```bash
# Google Apps Script API Configuration
NEXT_PUBLIC_API_URL=https://script.google.com/macros/s/AKfycbyPQ50mUvaYqvcat63sIpDfVJwBXiQFNnlvvL-8sd2WTW8MkAtsC195EAHHn1WuQWer/exec
NEXT_PUBLIC_API_KEY=xK8+3mP9qR4vN2jL5tW7aZ1cY6eB0fH8
```

**หมายเหตุ:** ใช้ API Key เดียวกันกับที่ตั้งใน Script Properties

### ขั้นที่ 5: อัพเดต Config Sheet ใน Google Sheets

เปิด Sheet ชื่อ **Config** และอัพเดตแถว API_KEY:

| Key | Value |
|-----|-------|
| API_KEY | xK8+3mP9qR4vN2jL5tW7aZ1cY6eB0fH8 |

**ต้องเป็นค่าเดียวกันทั้ง 3 ที่:**
1. Script Properties (`API_KEY`)
2. ไฟล์ `.env.local` (`NEXT_PUBLIC_API_KEY`)
3. Config Sheet (`API_KEY`)

### ขั้นที่ 6: Restart Development Server

```bash
# หยุด server (กด Ctrl+C)
# เริ่ม server ใหม่
npm run dev
```

### ขั้นที่ 7: อัพเดท URL ในไฟล์ .env.local

**สำคัญ!** ถ้า Deploy ใหม่ URL อาจเปลี่ยน:

```bash
NEXT_PUBLIC_API_URL=https://script.google.com/macros/s/NEW_DEPLOYMENT_ID/exec
NEXT_PUBLIC_API_KEY=xK8+3mP9qR4vN2jL5tW7aZ1cY6eB0fH8
```

### ขั้นที่ 8: Restart Development Server

```bash
# หยุด server (กด Ctrl+C)
# เริ่ม server ใหม่
npm run dev
```

### ขั้นที่ 9: ทดสอบการเชื่อมต่อ

1. เปิด http://localhost:3000
2. ตรวจสอบว่าข้อมูลโหลดขึ้นมาได้หรือไม่
3. ถ้ายังมี error ให้เปิด Developer Console (F12) ดู error message

---

## 📝 วิธีแก้ไขแบบละเอียด ทีละขั้นตอน

### ✅ Checklist การ Deploy Google Apps Script

- [ ] 1. เปิด Google Sheet
- [ ] 2. Extensions > Apps Script
- [ ] 3. เพิ่มฟังก์ชัน `doOptions(e)` ในโค้ด
- [ ] 4. บันทึกไฟล์ (Ctrl+S หรือ File > Save)
- [ ] 5. Deploy > Manage deployments
- [ ] 6. Edit deployment
- [ ] 7. New version
- [ ] 8. **Execute as: Me**
- [ ] 9. **Who has access: Anyone** ⬅️ สำคัญ!
- [ ] 10. Deploy
- [ ] 11. Copy Web app URL
- [ ] 12. ทดสอบ URL ในเบราว์เซอร์

### ✅ Checklist การตั้งค่า API Key

- [ ] 1. สร้าง API Key (ใช้ PowerShell command)
- [ ] 2. เพิ่มใน Script Properties (`API_KEY`)
- [ ] 3. เพิ่มใน `.env.local` (`NEXT_PUBLIC_API_KEY`)
- [ ] 4. เพิ่มใน Config Sheet (แถว `API_KEY`)
- [ ] 5. API Key ทั้ง 3 ที่ต้องเหมือนกันทุกประการ
- [ ] 6. Restart server

---

## 🎯 วิธีแก้ไขแบบด่วน (Quick Fix)

ถ้าต้องการทดสอบระบบก่อน สามารถใช้ API Key แบบง่ายๆ:

### 1. ใน Google Apps Script - Script Properties
```
API_KEY = my-secret-key-2024
```

### 2. ในไฟล์ .env.local
```bash
NEXT_PUBLIC_API_KEY=my-secret-key-2024
```

### 3. ใน Config Sheet
```
API_KEY | my-secret-key-2024
```

### 4. Restart Server
```bash
npm run dev
```

---

## การตรวจสอบว่าใช้งานได้แล้ว

1. เปิด http://localhost:3000
2. Dashboard ควรแสดงข้อมูล:
   - Total Cars
   - Available Cars
   - Total Bookings
   - แผนภูมิต่างๆ

3. ไม่มี error ใน Console (กด F12)

---

## ข้อควรระวัง

⚠️ **อย่าใช้ API Key เดียวกันใน Production!**

สำหรับ Production (Vercel):
1. สร้าง API Key ที่แข็งแรงกว่า
2. ตั้งค่าใน Vercel Environment Variables
3. ไม่ต้อง commit .env.local ไป GitHub

---

## ถ้ายังไม่ได้

### ตรวจสอบเพิ่มเติม:

1. **ตรวจสอบ URL ของ Google Apps Script**
   - ต้องเป็น URL ที่ได้จากการ Deploy (ไม่ใช่ URL แก้ไขโค้ด)
   - ต้องมี `/exec` ต่อท้าย

2. **ตรวจสอบว่า Deploy แล้ว**
   - ใน Apps Script: Deploy > Manage deployments
   - Status ต้องเป็น Active
   - "Who has access" ต้องเป็น **Anyone**

3. **ตรวจสอบ CORS**
   - โค้ดใน Code.gs มี CORS headers อยู่แล้ว
   - ถ้ายังไม่ได้ลอง Redeploy Apps Script

4. **ล้าง Cache**
   ```bash
   # หยุด server
   # ลบ .next folder
   Remove-Item -Recurse -Force .next
   # เริ่มใหม่
   npm run dev
   ```

---

## สรุปขั้นตอน

1. ✅ สร้าง API Key ที่แข็งแรง
2. ✅ ตั้งค่าใน Script Properties
3. ✅ อัพเดตไฟล์ .env.local
4. ✅ อัพเดต Config Sheet
5. ✅ Restart development server
6. ✅ ทดสอบการเชื่อมต่อ

---

**ถ้าทำตามขั้นตอนแล้วยังไม่ได้ แจ้งมาได้เลยครับ จะช่วยแก้ต่อ! 🚀**
